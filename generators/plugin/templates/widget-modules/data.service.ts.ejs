export function <%= widgetDataServiceName %>(
  $q: angular.IQService,
  <%= widgetConstantsName %>: any
) {
  const service = {
    getData
  };

  return service;

  ////////////

  function getData(filter: c8y.WidgetDataFilter): Promise<%- '<any>' %> {
    return $q.resolve([]).then(data => {
      console.dir(data);
      return data;
    });
  }
}
