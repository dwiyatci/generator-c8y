(() => {
  'use strict';

  angular.module('<%= moduleName %>').factory('<%= widgetDataServiceName %>', dataService);

  function dataService($q: angular.IQService, <%= widgetConstantsName %>: any) {
    'ngInject';

    const service = {
      getData
    };

    return service;

    ////////////

    function getData(filter: c8y.WidgetDataFilter): Promise<%- '<any>' %> {
      return $q.resolve([]).then(data => {
        console.dir(data);
        return data;
      });
    }
  }
})();
