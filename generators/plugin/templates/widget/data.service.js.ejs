(() => {
  'use strict';

  angular.module('<%= moduleName %>').factory('<%= widgetDataServiceName %>', dataService);

  function dataService($q, <%= widgetConstantsName %>) {
    'ngInject';

    const service = {
      getData
    };

    return service;

    ////////////

    function getData({ source }) {
      return $q.resolve([]).then(data => {
        console.dir(data);
        return data;
      });
    }
  }
})();
