(function () {
  'use strict';

  angular
    .module('<%= moduleName %>')
    .component('<%= widgetConfigComponentName %>', {
      templateUrl: ':::PLUGIN_PATH:::/widget-config.html',
      bindings: {
        config: '<'
      },
      controller: Controller,
      controllerAs: 'vm'
    });

  /* @ngInject */
  function Controller() {
    const vm = this;

    _.assign(vm, {
      $onInit,
      $onChanges
    });

    ////////////

    function $onInit() {
      _.defaults(vm.config);
    }

    function $onChanges({ config }) {
      if (config && vm.config) {
        const { device } = vm.config;

        console.log(device);
      }
    }
  }
}());
