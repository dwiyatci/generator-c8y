export function <%= widgetDataServiceName %>(
  $q: angular.IQService,
  <%= widgetConstantsName %>: any
) {
  const service = {
    getDataFor
  };

  return service;

  ////////////

  function getDataFor(filter: c8y.WidgetDataFilter): Promise<%- '<any>' %> {
    return $q.resolve([])
      .then((data) => {
        console.log(data);
        return data;
      });
  }
}
