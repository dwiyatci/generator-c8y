(() => {
  'use strict';

  angular
    .module('<%= moduleName %>')
    .factory('<%= widgetDataServiceName %>', dataService);

  /* @ngInject */
  function dataService(
    $q,
    <%= widgetConstantsName %>
  ) {
    const service = {
      getDataFor
    };

    return service;

    ////////////

    async function getDataFor({ source }) {
      const data = await $q.resolve([]);

      console.log(data);

      return data;
    }
  }
})();
