(function () {
  'use strict';

  angular
    .module('<%= moduleName %>')
    .component('<%= prefixedComponentName %>', {
      template: `
        <div>{{vm.prop}}</div>
      `,
      bindings: {
        prop: '<'
      },
      controllerAs: 'vm',
      controller: Controller
    });

  /* @ngInject */
  function Controller() {
    const vm = this;

    _.assign(vm, {
      $onInit,
      $onChanges
    });

    ////////////

    function $onInit() {

    }

    function $onChanges({ prop }) {
      if (prop && vm.prop) {

      }
    }
  }
}());
