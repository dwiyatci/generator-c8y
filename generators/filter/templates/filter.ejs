(function () {
  'use strict';

  angular
    .module('<%= moduleName %>')
    .filter('<%= filterName %>', <%= filterName %>);

  function <%= filterName %>() {
    return filter;

    ////////////

    function filter(input) {
      return input;
    }
  }
}());
