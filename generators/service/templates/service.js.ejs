(() => {
  'use strict';

  angular.module('<%= moduleName %>').factory('<%= serviceName %>', <%= serviceName %>);

  function <%= serviceName %>() {
    'ngInject';

    const service = {
      method
    };

    return service;

    ////////////

    function method() {
      return 'hello, world';
    }
  }
})();
