(function () {
  'use strict';

  angular
    .module('<%= moduleName %>')
    .factory('<%= widgetDataServiceName %>', dataService);

  /* @ngInject */
  function dataService(
    $q,
    <%= widgetConstantsName %>
  ) {
    const service = { getDataFor };

    return service;

    ////////////

    function getDataFor({ source }) {
      return $q.resolve([])
        .then((data) => {
          console.log(data);

          return data;
        });
    }
  }
}());
