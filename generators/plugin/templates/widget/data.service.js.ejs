(function () {
  'use strict';

  angular
    .module('<%= moduleName %>')
    .factory('<%= widgetDataServiceName %>', dataService);

  /* @ngInject */
  function dataService(
    <%= widgetConstantsName %>,
    <%= widgetFakeDataServiceName %>
  ) {
    const service = {
      getDataForDeviceId
    };

    return service;

    ////////////

    function getDataForDeviceId(id) {
      const dataPromise = <%= widgetFakeDataServiceName %>.getDataForDeviceId(id);

      return dataPromise
        .then((data) => {
          console.log(data);

          return data;
        });
    }
  }
}());
