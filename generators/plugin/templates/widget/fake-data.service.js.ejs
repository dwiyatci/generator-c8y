(function () {
  'use strict';

  angular
    .module('<%= moduleName %>')
    .factory('<%= widgetFakeDataServiceName %>', fakeDataService);

  /* @ngInject */
  function fakeDataService(
    $http,
    c8yBase
  ) {
    const service = {
      getDataForDeviceId
    };

    return service;

    ////////////

    async function getDataForDeviceId(id) {
      const data = await $http.get(':::PLUGIN_PATH:::/fake-data.json').then(c8yBase.getResData);

      return data;
    }
  }
}());
