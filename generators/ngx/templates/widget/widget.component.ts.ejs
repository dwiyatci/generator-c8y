import { Component, Input, OnInit } from '@angular/core';
import { cloneDeep } from 'lodash-es';

import { <%= pascalCasedWidgetName %>WidgetService } from './<%= kebabCasedWidgetName %>-widget.service';

@Component({
  selector: 'c8y-<%= kebabCasedWidgetName %>-widget',
  templateUrl: './<%= kebabCasedWidgetName %>-widget.component.html',
  styles: [require('!raw-loader!less-loader!./styles.less')]
})
export class <%= pascalCasedWidgetName %>WidgetComponent implements OnInit {
  config = {};

  @Input('config')
  set _config(config) {
    this.config = cloneDeep(config);
  }

  constructor(private <%= camelCasedWidgetName %>Service: <%= pascalCasedWidgetName %>WidgetService) {}

  ngOnInit() {
    this.<%= camelCasedWidgetName %>Service.getData().subscribe(console.log);
  }
}
