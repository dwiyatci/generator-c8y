(() => {
  'use strict';

  angular.module('<%= moduleName %>').factory('<%= serviceName %>', <%= serviceName %>);

  /* @ngInject */
  function <%= serviceName %>() {
    const service = {
      method
    };

    return service;

    ////////////

    function method() {
      return 'hello, world';
    }
  }
})();
